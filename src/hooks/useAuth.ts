/**
 * Hook de autenticação — re-exporta do AuthProvider centralizado.
 * @deprecated Importe useAuth de @/providers/AuthProvider
 */
"use client";

export { useAuth } from "@/providers/AuthProvider";
